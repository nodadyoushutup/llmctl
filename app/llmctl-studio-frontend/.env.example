# Keep empty to use same-origin requests routed through ingress (/web + /api).
VITE_API_BASE_URL=

# Backend API namespace expected by the split deployment.
VITE_API_BASE_PATH=/api

# Frontend mount path for BrowserRouter basename.
VITE_WEB_BASE_PATH=/

# Socket.IO transport path and namespace for realtime features.
VITE_SOCKET_PATH=/api/socket.io
VITE_SOCKET_NAMESPACE=/rt

# Optional: local dev proxy target so /api requests from Vite dev server
# resolve without browser CORS requirements.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:5055
