apiVersion: v1
kind: Secret
metadata:
  name: llmctl-harbor-regcred
  namespace: llmctl
type: kubernetes.io/dockerconfigjson
data:
  # base64-encoded ~/.docker/config.json with Harbor auth for your registry endpoint.
  .dockerconfigjson: ""
