apiVersion: v1
kind: Service
metadata:
  name: llmctl-studio-frontend
  namespace: llmctl
spec:
  type: NodePort
  selector:
    app: llmctl-studio-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30157
