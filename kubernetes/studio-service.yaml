apiVersion: v1
kind: Service
metadata:
  name: llmctl-studio-backend
  namespace: llmctl
spec:
  type: NodePort
  selector:
    app: llmctl-studio-backend
  ports:
    - name: http
      port: 5155
      targetPort: http
